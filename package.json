{
  "name": "openlearn",
  "private": true,
  "packageManager": "pnpm@10.15.0",
  "engines": {
    "node": ">=24.0.0 <25.0.0"
  },
  "scripts": {
    "build": "pnpm --recursive run build",
    "build:packages": "pnpm --filter @openlearn/domain run build && pnpm --filter @openlearn/application run build && pnpm --filter @openlearn/mcp run build",
    "lint": "node scripts/quality-gates.mjs source",
    "typecheck": "pnpm run build:packages && pnpm --recursive run typecheck",
    "test": "pnpm --recursive run test",
    "verify": "pnpm run lint && pnpm run typecheck && pnpm run test && pnpm run build"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
